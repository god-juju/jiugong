# 九宮表單資料收集設定

目前已加上「Cloudflare Pages + D1 資料庫」的輕量後台。

Google 表單仍保留為備援，避免正式資料庫還沒設定完成時收不到資料。

- 填表人看的畫面：`index.html`
- 老闆看的後台：`admin.html`
- 新資料庫設定：`後台設定.md`
- 收資料後台：[Google 表單](https://docs.google.com/forms/d/e/1FAIpQLSfFJzhqjmfbtlmODl7tq2BkfgByg8k4YYpL_agKBUvY509mEw/viewform)
- 回覆整理：已連到 Google 試算表

## 後台欄位對照

因為 Google 表單頁面不容易直接輸入中文，後台欄位用簡短代號；正式中文欄位都在 `index.html` 裡。

| Google 表單欄位 | 實際代表資料 |
|---|---|
| Q1 | 門生姓名（中文） |
| Q2 | 行動電話 |
| Q3 | 電子郵件 |
| Q4 | Line ID |
| Q5 | 完整資料備份 |

其中 `Q5` 會包含填表人所有完整資料，包含五個部分的全部回答。

## 使用方式

1. 打開 `index.html`。
2. 填完表單後按送出。
3. 如果 Cloudflare 後台已設定完成，畫面會顯示「資料已送出成功」。
4. 如果 Cloudflare 後台尚未完成，會改走 Google 表單備援，看到「已記錄你的回覆」才代表真的送出成功。
5. 正式後台資料會在 `admin.html` 查看；Google 表單只當備援。

## 重要提醒

不要刪除 Google 表單裡的 Q1 到 Q5，也不要再開啟「僅限回覆 1 次」，否則 `index.html` 送出會失敗或無法收第二筆資料。
