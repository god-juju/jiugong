# 九宮資料後台設定

這份文件是給之後正式上線用的。

目前專案新增了：

- `index.html`：填表頁，會優先送到新資料庫
- `admin.html`：老闆登入後台
- `functions/api/*`：收資料、登入、讀資料的後台程式
- `schema.sql`：資料庫欄位表

## 為什麼不能只用 GitHub Pages

GitHub Pages 只能放靜態網頁。

白話說，它只能顯示表單，不能幫我們：

- 存資料到資料庫
- 判斷老闆有沒有登入
- 讓老闆登入後看資料

所以要有「資料庫 + 登入」，建議改用 Cloudflare Pages。

## 建議正式網址

目前已部署到 Cloudflare Pages。

填表頁：

`https://jiugong-cmj.pages.dev/`

老闆後台：

`https://jiugong-cmj.pages.dev/admin.html`

目前 Cloudflare Pages 專案名稱：

`jiugong`

目前 D1 資料庫名稱：

`jiugong-db`

## Cloudflare Pages 設定步驟

1. 到 Cloudflare 後台。
2. 進入 `Workers & Pages`。
3. 建立新的 Pages 專案。
4. 選 GitHub repo：`god-juju/jiugong`。
5. 建置設定：
   - Framework preset：選 `None` 或不要選框架
   - Build command：留空
   - Build output directory：留空；如果 Cloudflare 要求填寫，就填 `/`
6. 部署一次。

目前本專案已用 Wrangler 直接部署過一次。之後如果有修改，可以在這個資料夾執行：

`npx wrangler pages deploy . --project-name jiugong --branch main --commit-dirty=true`

## 建立 D1 資料庫

D1 是 Cloudflare 的小型資料庫，適合這種登記表使用。

1. 到 Cloudflare 後台。
2. 進入 `Workers & Pages`。
3. 找到 `D1 SQL Database`。
4. 建立資料庫，建議名稱：

`jiugong-db`

## 把資料庫接到 Pages 專案

1. 回到剛剛建立的 Pages 專案。
2. 進入 `Settings`。
3. 找到 `Bindings`。
4. 新增 D1 database binding。
5. Variable name 必須填：

`DB`

6. D1 database 選剛剛建立的：

`jiugong-db`

7. 儲存後重新部署。

## 設定老闆登入密碼

到 Pages 專案的 `Settings`，找到 `Variables and Secrets`。

新增兩個變數：

| 名稱 | 用途 |
|---|---|
| `ADMIN_PASSWORD` | 老闆登入後台用的密碼，至少 16 個字 |
| `COOKIE_SECRET` | 保護登入狀態用的亂數文字，至少 24 個字 |

`COOKIE_SECRET` 可以填一串很長、沒規則的文字，例如 30 個以上英數字混合。

這兩個不要寫進 `index.html`，只放在 Cloudflare 後台。

後台登入如果連續錯 5 次，會暫時擋 15 分鐘，避免有人一直猜密碼。

## 資料庫欄位

程式會自動建立資料表。

如果 Cloudflare 後台需要手動建立，可以把 `schema.sql` 的內容貼到 D1 的 SQL Console 執行。

## 使用方式

填表人：

1. 打開正式填表網址。
2. 填完送出。
3. 畫面看到「資料已送出成功」就完成。

老闆：

1. 打開 `/admin.html`。
2. 輸入後台密碼。
3. 可以看列表、搜尋資料、點開完整內容、匯出 CSV。

## 目前的保護措施

- 老闆後台需要密碼
- 登入狀態存在安全 cookie 裡
- 密碼不會寫在網頁原始碼
- 填表資料會存在 D1 資料庫
- 在正式後台還沒完成前，`index.html` 仍保留 Google 表單備援，避免資料收不到

## 上線前檢查

1. 用測試資料填一次表單。
2. 確認填表頁顯示「資料已送出成功」。
3. 打開 `/admin.html`。
4. 用老闆密碼登入。
5. 確認看得到剛剛測試那筆資料。
6. 匯出 CSV，確認中文正常。
